# Example config.txt — copy to microSD (/sdcard) or USB MSC (/storage).
# wifi_ssid=MyNetwork
# wifi_password=secret
# ain_pins=1             # GPIO(s) for espd/ain/0.. (requires ESPD_USE_AIN)
# ain_task_period_ms=5
# ain_deadband=32
# touch_pins=4,5
# touch_task_period_ms=5
# aout_pins=8,9          # PWM GPIOs for espd/aout/0.. (requires ESPD_USE_AOUT)
# aout_pwm_freq_hz=19531
# din_pins=4,5           # extra espd/din after BSP buttons (requires ESPD_USE_DIN)
# din_active_low=1
# dout_pins=8,9          # GPIO digital out espd/dout/0.. (requires ESPD_USE_DOUT)
# audio_sample_rate=48000  # Hz (8000..192000); default from Kconfig
# audio_dma_desc_num=4
# audio_dma_frame_num=128
