version: "0.1.0"
description: ESPD adapter for managed esp-bsp packages

dependencies:
  idf: ">=6.0.1,<6.1"

  espd_bsp_selector:
    path: ../espd_bsp_selector

  espd_integration:
    path: ../espd_integration
