diff --git a/src/d_filter.c b/src/d_filter.c
index 401f5f46..68ae1ba6 100644
--- a/src/d_filter.c
+++ b/src/d_filter.c
@@ -5,6 +5,12 @@
 /*  "filters", both linear and nonlinear.
 */
 #include "m_pd.h"
+#ifdef ESP_PLATFORM
+#include "esp_attr.h"
+#endif
+#ifndef IRAM_ATTR
+#define IRAM_ATTR
+#endif
 
 /* ---------------- hip~ - 1-pole 1-zero hipass filter. ----------------- */
 
@@ -49,7 +55,7 @@ static void sighip_ft1(t_sighip *x, t_floatarg f)
         x->x_cspace.c_coef = 1;
 }
 
-static t_int *sighip_perform(t_int *w)
+static IRAM_ATTR t_int *sighip_perform(t_int *w)
 {
     t_sample *in = (t_sample *)(w[1]);
     t_sample *out = (t_sample *)(w[2]);
@@ -60,7 +66,7 @@ static t_int *sighip_perform(t_int *w)
     t_sample coef = c->c_coef;
     if (coef < 1)
     {
-        t_sample normal = 0.5*(1+coef);
+        t_sample normal = 0.5f*(1.f+coef);
         for (i = 0; i < n; i++)
         {
             t_sample new = *in++ + coef * last;
@@ -167,7 +173,7 @@ static void siglop_clear(t_siglop *x, t_floatarg q)
     x->x_last = 0;
 }
 
-static t_int *siglop_perf_scalar(t_int *w)
+static IRAM_ATTR t_int *siglop_perf_scalar(t_int *w)
 {
     t_siglop *x = (t_siglop *)(w[1]);
     t_sample *in1 = (t_sample *)(w[2]);
@@ -322,7 +328,7 @@ static void sigbp_clear(t_sigbp *x, t_floatarg q)
     x->x_cspace.c_x1 = x->x_cspace.c_x2 = 0;
 }
 
-static t_int *sigbp_perform(t_int *w)
+static IRAM_ATTR t_int *sigbp_perform(t_int *w)
 {
     t_sample *in = (t_sample *)(w[1]);
     t_sample *out = (t_sample *)(w[2]);
